# STM32 USB 연결 문제 해결 과정 요약

## 목표
- Linux 환경에서 STM32(JZ-F407ET6) 보드를 연결하여 통신하는 것.

## 진행 과정

1.  **초기 상태**: 보드를 USB로 연결했으나, 시리얼 통신이 되지 않음.

2.  **문제 진단 (1차)**: `lsusb`를 통해 확인 결과, 보드가 프로그래밍 모드인 `Mass Storage Device`로 인식됨. 이는 펌웨어가 없거나 잘못된 상태임을 의미.

3.  **펌웨어 업로드 시도**: 컴파일된 `submarine-test.bin` 파일을 저장 장치로 복사하여 펌웨어를 업로드함.

4.  **문제 지속**: 펌웨어 업로드 및 보드 리셋 후에도 가상 시리얼 포트(`/dev/ttyACM*`)가 생성되지 않음. 보드가 계속 `Mass Storage Device`로 남거나 `lsusb` 목록에서 사라짐.

5.  **문제 진단 (2차 - 원인 파악)**: 펌웨어 소스 코드(`main.c`)를 분석한 결과, USB 기능 자체는 코드에 포함되어 있었음. 하지만 **시스템 클럭 설정**이 실제 보드 하드웨어와 맞지 않는 것을 발견.
    -   **원인**: 프로젝트가 **25MHz** 외부 크리스탈 기준으로 설정되어 있었으나, 실제 JZ-F407ET6 보드는 **8MHz** 크리스탈을 사용함. 이 차이로 인해 USB 주변 장치가 올바르게 초기화되지 못함.

6.  **해결 시도 (1차 - 코드 수정)**: `main.c`의 `SystemClock_Config` 함수를 8MHz 크리스탈 기준으로 수정함.

7.  **컴파일 오류 발생**: 수정한 코드를 재컴파일하는 과정에서 `arm-none-eabi-gcc: not found` 오류 발생.
    -   **원인**: ARM 툴체인(컴파일러)이 설치는 되어 있으나, 시스템 `PATH`에 등록되지 않아 찾지 못함.

8.  **해결 시도 (2차 - 컴파일 성공)**: 시스템에 설치된 컴파일러의 전체 경로를 직접 지정하여 재컴파일에 성공함.

9.  **최종 펌웨어 업로드 문제**: 새로 컴파일된 펌웨어를 보드에 업로드하는 과정에서 `Not a directory`, `Permission denied` 등 리눅스 명령어 관련 문제가 계속 발생함.
    -   **원인**: 보드의 ST-Link가 제공하는 `Mass Storage` 기능이 표준적인 방식으로 동작하지 않아 파일 복사가 매우 까다로움.

10. **대안 모색**: 사용자가 제안한 **아두이노를 USB-to-Serial 변환기로 사용**하는 방법을 다음 해결책으로 채택하기로 함. 이는 문제가 되고 있는 STM32의 USB 기능을 완전히 우회하는 안정적인 방법임.

## 다음 단계
- `main.c` 코드를 수정하여 USB가 아닌 UART(시리얼 포트)로 통신하도록 변경.
- 아두이노와 STM32 보드를 점퍼선으로 연결하여 통신 확인.
